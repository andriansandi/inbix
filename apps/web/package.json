{
  "name": "@inbix/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config ../../wrangler.jsonc --port 8791",
    "build": "wrangler deploy --dry-run --config ../../wrangler.jsonc",
    "deploy": "wrangler deploy --config ../../wrangler.jsonc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "tail": "wrangler tail --config ../../wrangler.jsonc"
  },
  "dependencies": {
    "@clerk/backend": "^3.8.4",
    "@inbix/database": "workspace:*",
    "@inbix/parser": "workspace:*",
    "@inbix/shared": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.8.0",
    "wrangler": "^3.99.0"
  }
}
